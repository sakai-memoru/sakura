//キーボードマクロのファイル
S_Paste(0);	// 貼り付け
S_Up(0);	// カーソル上移動
S_Up(0);	// カーソル上移動
S_Up(0);	// カーソル上移動
S_Up(0);	// カーソル上移動
S_Up(0);	// カーソル上移動
S_Up(0);	// カーソル上移動
S_Up(0);	// カーソル上移動
S_Up(0);	// カーソル上移動
S_Up(0);	// カーソル上移動
S_Up(0);	// カーソル上移動
S_Right(0);	// カーソル右移動
S_Right(0);	// カーソル右移動
